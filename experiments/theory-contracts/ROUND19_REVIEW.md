# Round19 independent review and integration record

2026-09-07. NON-RH. This records bounded research review, not formal proof
assistant certification, empirical confirmation or T1–T8 completion.

## Independent mathematical interfaces

- Root read the entire observable, source-domain and cubic-selection proofs
  and their checker implementations. The TT polynomial uses actual staggered
  adjoints and retains means explicitly. Finite gauge orbits use complete
  frames and signed charge labels; no unsigned-axis quotient is promoted.
- The independent observable lane reviewed the coupled parent and all source
  domain/clock sections: graph-summable direct sums, common positive gap,
  countable-tail convergence and weighted charged clock readouts agree.
- The independent charge lane reviewed the coupled parent's frame extension
  and entanglement derivation. In the complete (f,q,h) chart, source operators
  depend only on q and scalar variables. Gauge projectors act on finite f;
  frame locks on finite h. They commute with source closures and their forms.
  Unbounded alignment and gradient penalties remain in the electric fiber;
  they are not mislabeled as bounded finite-register perturbations.
- The second-order purity calculation does not assume [B,R]=0. Its coefficient
  is lambda^2 Var(R)/2, equal to lambda^2/(4N) for the stated scalar Gaussian.
  It concerns base-generator time, not automatically the quadratic clock.
- The source force term is retained off the selected uniform sectors. The
  mass-substituted old vertex concerns the limiting TT/matter model; the
  finite-parameter base retains its distinct first jet.
- Spectator cancellation is limited to suitable base-time correlations.
  Nonzero-duration transition words and sqrt-clock energies keep their
  extra factor. Optional homogeneous compression is not a *-homomorphism
  of the full unprojected local algebra.

## A real integration failure found before claiming success

The first root coupling checker rejected its prerequisite. The prerequisite
itself passed: Round18 emits `new_exact_check_groups`, while the new caller
incorrectly requested `exact_new_check_groups`. Systematic debugging traced
the failure to this interface, not to a failed physical identity. The new
caller was corrected to the actual pinned schema and also requires the full
66-record inventory. No old source or expected physical value was changed.

Afterward independent normal and optimized runs of the coupling checker
passed. Its electric-transition test additionally recomputes an actual
one-edge charge increment and both scalar endpoint coefficients; it does
not substitute an unrelated scalar Hessian check for reciprocal response.

## Negative controls and optimization

The observable and source-domain lanes each mutated an imported copy of the
actual Ward source and observed rejection under optimized Python from a
foreign working directory. Their built-in exact controls also reject
anisotropic TT kinetic contamination, force-free inhomogeneous momentum,
the old stress at a shifted uniform mass, unweighted charged clock shifts,
and multiplicative interpretation of homogeneous compression.

The cubic lane checks the failed unsigned quotient, retained flat holonomies,
off-shell cubic failure of the outgoing-only quartic, complete normalized
orbits, full signed cocycle and the difference from native edge gauge action.
The coupled checker pins six predecessor inputs and reruns the complete
Round18 charge chain. Runner fixtures independently mutate only temporary
predecessor files and confirm that missing or changed sources fail even
under optimization; real research files are never mutated by these tests.

The 22 isolated runner tests pass normally and under `-OO`. These are
infrastructure cases, separate from new exact scientific regression groups,
source provenance checks and rerun prerequisites. Counts must not be summed
into a claimed number of mathematical theorems or TOE requirements closed.

## Preservation and scope

Only new Round19 artifacts and the three research navigation/index files
are integrated by this round. Prior untracked research is preserved. The
concurrent paper source, v472 script and empirical ledger are untouched;
there is no release, commit or push. The full microscopic TFPT/T1–T8
conjunction remains open despite the new common declared construction.
