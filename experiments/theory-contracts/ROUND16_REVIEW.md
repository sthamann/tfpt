# Round16 internal review record

2026-09-06. This records agent-assisted mathematical and source review of
bounded NON-RH research. It is not external peer review, a proof-assistant
certificate, empirical confirmation or a full physical gate promotion.

## Division of work and independent checks

- The clock/vertex author developed the measurable characteristic gauge and
  full joint model. The root independently checked the entire proof and
  authored its checker; the author independently read and reran that checker.
  The folder's [detailed review](clock-vertex-round16/REVIEW.md) records the
  exceptional-set, cocycle, domain and physical-trace audit.
- The spectator/clock author developed the positive lower bound, functional
  calculus, weighted isometries and normalized finite-energy approximation.
  The root and clock author independently checked the complete proof and
  its bounds. The root also independently read the complete checker.
- The charged-lift author developed the exact neutral splitting, cocycle
  trivialization, full shift representation and finite-code lift. The root
  independently checked all those formulas, energy domains and claim limits.
  The charged-lift author additionally reviewed the entire clock/vertex
  proof and reran its checker; no further defect was found.

## Load-bearing points explicitly reviewed

1. The characteristic unitary represents the SAME existing H_vp. Its full
   unitary-group identity, not formal cancellation of derivatives, proves
   the domain and generator equality. Bare K_c and instantaneous h(c) are
   not misidentified as the transported commuting operators.
2. The exceptional set bv=0 is a proper null subspace, not only the origin.
   Radial strong convergence and fixed-shape density domination justify
   the Gaussian trace. A scalar curved-path control rejects unrestricted
   continuity. No multiplication of delta(c) by a measurable coefficient
   is used. Unitary cancellation gives the absolute joint Haar integral.
3. The quadratic clock keeps its exact factor six and energy half-density.
   A noncommuting physical projector cannot be moved through that weight.
   The transported clock tube differs from the raw old spectral tube.
4. The local parent obeys B_delta,eta>=e_m after its declared subtraction:
   use the displaced oscillator's pointwise lower bound, then the actual
   matter form, without assuming its kinetic term preserves fast vacua.
   C0 functional calculus and energy-tail cutoffs transfer full dynamics.
5. Weighted clock embeddings are exact isometries, not exact finite-parameter
   dynamical intertwiners. The proof wording explicitly separates the
   zero iterated LIMIT of the error from its generally nonzero finite value.
6. The Gaussian spectator error holds for every initial physical Hilbert
   vector. Its energy and position costs, changing embeddings, no L2 zero
   vacuum, nonfactorizing square root, and finite-delta symmetry failure
   are retained. Exact spectator constraint reduction is optional and
   limit-only, not a hidden change to the finite parent.
7. The neutral lattice decomposition is integral and bijective, including
   negative winding. The phase redefinition simplifies only the s step;
   other shifts retain the j-dependent cocycle. The fourth power is a
   bilateral neutral translation, not a finite periodic register.
8. The finite-code multiplicity survives the explicit unitary lift. The
   global charge energy and its cross terms are new physical input; the
   unbounded cost of charged steps and the transverse-cut defect remain.
9. Systematic debugging traced a checker performance failure to generic
   symbolic inversion of unsimplified rational matrix entries. The replacement
   checks the exact all-delta inverse-action equation and independently
   inverts the full matrix at rational delta. No spectral channel, source
   term or analytic proof hypothesis was removed to make the test pass.

## Reproducibility and preservation

New checkers use explicit failure exceptions, not removable assertions.
Their integrated runs and six unchanged prerequisites are recorded separately
from twenty normal and twenty optimized infrastructure fixtures. Manifest
validation checks current inventories, digests and recorded successful runs;
it is not proof correctness or a signed attestation. The prior Round10--15
artifacts are not rewritten to retrofit new conclusions. Concurrent paper,
status-ledger and v472 changes are outside this round's edits.

The correct conclusion is finite constructive compatibility plus two
explicit mathematical extensions, with unresolved microscopic selection
and common-parent obligations. No T1--T8, RH or empirical closure follows.
